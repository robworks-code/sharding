{
  "name": "sharding",
  "version": "0.0.8",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "verify": "npm run typecheck && npm run build && npm test",
    "prepush": "npm run verify && node scripts/check-dist-committed.mjs",
    "cli": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "yaml": "^2.4.0"
  }
}
